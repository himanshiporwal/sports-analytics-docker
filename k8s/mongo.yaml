apiVersion: apps/v1
kind: Deployment
metadata:
  name: sports-mongo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sports-mongo
  template:
    metadata:
      labels:
        app: sports-mongo
    spec:
      containers:
        - name: mongo
          image: mongo:6
          ports:
            - containerPort: 27017
---
apiVersion: v1
kind: Service
metadata:
  name: sports-mongo
spec:
  selector:
    app: sports-mongo
  ports:
    - port: 27017
      targetPort: 27017